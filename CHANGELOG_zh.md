# Aves 中文更新日志（远程媒体扩展）

## 2026-03-31

### 新增
- 新增“远程媒体”设置分组。
- 新增远程日志体系与日志页面，支持：复制、导出 TXT、导出并分享、清空。
  - 导出日志时自动清理旧导出文件，避免缓存目录长期累积。
- 新增远程连接管理页面：
  - 新建/编辑/删除连接；
  - 支持 WebDAV、FTP、SFTP、SMB 协议配置；
  - 支持 FTP 匿名登录、主动/被动模式；
  - 支持 SFTP 私钥/口令/高级配置字段；
  - WebDAV 地址作为完整来源（包含端口时不再额外配置端口）。
- 新增远程目录浏览页：
  - 层级懒加载；
  - 当前层级搜索（文件夹+文件，支持名称与路径匹配）；
  - 手动固定/取消固定“在相册中显示”。
- 新增抽屉中的远程固定目录快捷入口。
- 新增远程预览策略决策：
  - 流式优先 / 下载优先判定；
  - 自动下载大小上限（图片/视频分开）；
  - 流式失败回退下载策略。
- 新增按连接缓存目录与“按连接清理缓存”能力。
- 新增 WebDAV 按需自动下载到连接缓存（受策略控制）。
- 扩展 FTP/SFTP/SMB 按需下载到连接缓存。
- 新增“流式不可用时自动回退下载”执行闭环。
- 新增“流式地址可用但实际打开失败”时的回退下载与自动重试打开。
- 新增下载完成后自动尝试打开远程媒体文件。
- 优化下载后打开逻辑为 `file://` URI，提升系统兼容性。
- 当“仅流式”与不支持直连流式的协议冲突时，增加明确引导提示（关闭仅流式或强制下载）。
- 新增 FTP/SFTP/SMB 真实认证连接测试（不再仅 TCP 探测）。
- 新增远程浏览面包屑层级导航，可直接跳转父层/上层。
- 新增远程浏览单页逐级返回链路（返回键逐层回退）。
- 新增远程连接列表缓存占用显示。
- 新增远程文件长按/更多操作菜单，支持：
  - 按策略打开；
  - 强制下载并打开；
  - 仅流式尝试。
- 新增目录项长按/更多操作：可直接“在远程相册中显示/取消显示”。
- 新增通过 `.nomedia` 控制远程缓存是否参与媒体扫描（受设置开关控制）。
- 新增测试构建脚本：
  - `app-arm64-v8a-izzy-release.apk` 快速构建脚本；
  - 全架构与版本化产物脚本。
  - 构建脚本新增 `pubspec.lock` 备份恢复，避免构建过程污染工作区。
  - 兼容 `build/app` 与 `android/app/build` 两种产物目录，统一 arm64 输出文件名。
  - 简化依赖拉取流程并保留 lock 恢复，减少构建噪音日志。

### 优化
- 优化单列预览卡片边距（上下左右更易区分）。
- 增强预览自动播放稳定性：
  - 去重防抖；
  - 焦点切换前后一致性校验；
  - 自动播放请求令牌机制，减少焦点抖动导致的误播/竞争。
  - 增加播放前就绪等待与一次自动重试，降低白屏/首帧延迟概率。
  - 增加延后二次重试，进一步改善部分视频（含横屏）首帧自动播放触发。
  - 二次重试前增加轻量 seek 刷新解码，缓解白屏后才出首帧问题。
- 增加跨条目抗抖节流窗口，降低焦点抖动导致的频繁竞争播放。
- 远程缓存媒体接入自动播放与默认声音设置判定（含日志字段）。
- CollectionPage 网格/马赛克预览新增视频自动播放链路，统一覆盖本地与远程缓存媒体，并保持同屏仅单视频播放。
- CollectionPage 预览自动播放新增焦点释放暂停与播放触发日志，便于排查抖动与竞争播放问题。
- CollectionPage 预览焦点计算增加底部回退取样，改善列表末尾视频自动播放触发稳定性。
- CollectionPage 自动播放视频缩略图新增静音/有声切换按钮，并记录切换日志。
- CollectionPage 单列预览下视频自动播放改为更接近详情页的展示比例（降低明显裁切）。
- 设置页新增本地网格/马赛克预览自动播放与默认声音开关，与远程预览设置并存且互不冲突。
- 增强远程加载与自动播放相关日志覆盖（加载、策略决策、连接测试、缓存行为）。
- 增强点击远程媒体时的策略与资源解析日志（stream/download 命中情况）。
- 增强远程目录进入/返回链路日志与打开失败原因日志（含 stream-only 失败、无可播放资源判定）。
- 远程浏览路径标准化处理增强，提升多层级进入/返回稳定性。
- Wi-Fi 限制页一键关闭策略动作纳入远程日志。
- 完善远程相关页面中英文文案覆盖。
- 补充远程连接编辑页 WebDAV 地址字段文案多语言切换。
- 远程加载失败提示改为多语言并提供一键重试。
- 远程连接列表协议副标题文案支持多语言切换。
- 远程连接编辑弹窗新增协议必填校验与端口范围校验，减少无效配置。
- 收紧 WebDAV URL 协议校验（http/https）并新增 SFTP 高级 JSON 格式校验。
- Wi-Fi 限制拦截页新增“一键关闭仅 Wi-Fi 并加载”快捷操作。
- 远程连接列表缓存占用改为按需缓存刷新，减少重复统计带来的卡顿。
- 优化“导出并分享日志”为分享导出的 TXT 文件（不再仅分享纯文本）。
- 当导出文件分享失败时自动回退为文本分享，提升日志分享成功率。
- 日志分享回退文本时的分享主题支持多语言。
- 远程固定目录入口统一显示末级目录名，更符合文件夹浏览习惯。

### 说明
- 本次为“本地 + 远程统一浏览器”能力的持续迭代版本，已完成远程接入基础链路与关键稳定性增强。
- 后续会继续推进远程预览播放链路的深度接入与更多协议下的流式能力细化。
